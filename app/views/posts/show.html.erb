<%- model_class = Post -%>
<div class="page-header">
  <h1 style="color:#fff;"><%= @post.author%>'s congratulation for <%= link_to @post.for_whom ,user_path(User.find(2)), :style=>'color:#fff;'%></h1>
</div>
<div class= "row">
<div class = "span8" style="margin-bottom:20px;" align="center">
 <div class="thumbnail" style="background:#fff;">
            <% if @post.image?%>
              <img src=<%=@post.image_url%>  class="img-rounded">

            <%end%>
            <p style="text-align:left;"><font class="lead" face="URW Chancery L" size="6px" color="4a5fff"><%= @post.congratulation %></font></p>
   </div>         <!--<%= link_to image_tag(@post.image_url(:thumb)), @post, :class => 'img-rounded' %>-->
  </div>         
</div>

  <a class="btn btn-primary" href=<%=posts_path%> style="color:#fff;margin-right=10px;"><i class="icon-arrow-left icon-white"></i>   Back</a>
  <!-- <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_post_path(@post), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              post_path(@post),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %> -->

